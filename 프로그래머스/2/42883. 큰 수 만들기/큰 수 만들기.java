import java.util.*;
class Solution {
    public String solution(String number, int k) {
        String ans = "";
        List<Integer> arr = new ArrayList<>();
        arr.sort(Integer:: compareTo);
        
        return ans;
    }
}